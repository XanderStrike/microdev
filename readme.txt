curl https://raw.githubusercontent.com/XanderStrike/microdev/refs/heads/main/dev.sh | bash

curl https://raw.githubusercontent.com/XanderStrike/microdev/refs/heads/main/dev.fish | fish

dev cd <name>         Jump to a project directory
dev clone <url>       Clone a repo into ~/workspace/ and cd into it
dev new <name>        Create a new project in ~/workspace/ with git init
dev install           Run the installer
